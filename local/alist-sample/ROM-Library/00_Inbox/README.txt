Local sample folder for AList testing.

In AList local admin, add a Local storage:
- Mount path: /ROM-Library
- Root folder path: /local-rom-library/ROM-Library

The ROM Manager rescan button should then see this file as an unclassified item.
